{
  "private": true,
  "name": "path-url",
  "version": "0.0.2",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oyb81076/url-path.git"
  },
  "scripts": {
    "test": "jest",
    "prebuild": "rm -rf build",
    "build": "tsc",
    "postbuild": "node scripts/build.js"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^12.0.12",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "tslint": "^5.18.0",
    "typescript": "^3.5.2"
  }
}
